# Existing properties
# ... existing code ...

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=ALWAYS
management.health.defaults.enabled=true

# External Service Connection Properties (for health checks)
# Kafka
kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
kafka.connection-timeout=${KAFKA_CONNECTION_TIMEOUT:2000}

# IBM MQ
ibmmq.host=${IBMMQ_HOST:localhost}
ibmmq.port=${IBMMQ_PORT:1414}
ibmmq.connection-timeout=${IBMMQ_CONNECTION_TIMEOUT:2000}

# Tibco EMS
tibco.server=${TIBCO_SERVER:localhost}
tibco.port=${TIBCO_PORT:7222}
tibco.connection-timeout=${TIBCO_CONNECTION_TIMEOUT:2000}

# ActiveMQ
activemq.broker-url=${ACTIVEMQ_BROKER_URL:localhost}
activemq.port=${ACTIVEMQ_PORT:61616}
activemq.connection-timeout=${ACTIVEMQ_CONNECTION_TIMEOUT:2000} 